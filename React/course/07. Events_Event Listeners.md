# Events
* React handles events in a similar way to DOM events, but with a few key differences to make it more consistent and declarative
* <img width="482" alt="image" src="https://github.com/user-attachments/assets/4688ed91-363c-4783-8652-9e22499c587f" />
# Key Concepts 
* **Syntax Difference**
  * **HTML (Vanilla JS):**
    ```html
    <button onclick="doSomething()">Click me</button>
    ```
  * **React (JSX):**
    ```jsx
    <button onClick={doSomething}>Click me</button>
    ```
* ✅ Note: React uses camelCase for event names (e.g., onClick, not onclick)
* ✅ The event handler is passed as a function, not a string       

# Common React Events
* ![image](https://github.com/user-attachments/assets/5960bb3a-1476-4a3e-bb01-d793ffad5e61)

# Examples
* **onClick**
  ```jsx
  function App() {
  const handleClick = () => {
    alert('Button clicked!');
  };
  
  return <button onClick={handleClick}>Click Me</button>;
  }
  ```
* **onChange (Input Field)**
  ```jsx
  function App() {
  const [name, setName] = React.useState('');

  return (
    <input
      type="text"
      value={name}
      onChange={(e) => setName(e.target.value)}
    />
  );
  }
  ```
* **onSubmit (Form)**
  ```
  function App() {
  const handleSubmit = (e) => {
    e.preventDefault(); // Prevent page reload
    alert('Form submitted!');
  };

  return (
    <form onSubmit={handleSubmit}>
      <button type="submit">Submit</button>
    </form>
  );
  }
  ```


# 📘 React Events Cheatsheet

React wraps browser events in a cross-browser wrapper called **SyntheticEvent**. You handle events in JSX using camelCase and pass a function, not a string.

---

## 🧠 Basic Syntax

```jsx
<button onClick={handleClick}>Click me</button>
```

---

## 🔹 Common Event Types

### 🔸 `onClick`
```jsx
<button onClick={() => alert('Clicked!')}>Click</button>
```

### 🔸 `onChange` (Input)
```jsx
<input type="text" onChange={(e) => console.log(e.target.value)} />
```

### 🔸 `onSubmit` (Form)
```jsx
<form onSubmit={(e) => {
  e.preventDefault();
  console.log('Form submitted');
}}>
  <button type="submit">Submit</button>
</form>
```

### 🔸 `onKeyDown`
```jsx
<input onKeyDown={(e) => console.log('Key:', e.key)} />
```

### 🔸 `onFocus` and `onBlur`
```jsx
<input onFocus={() => console.log('Focused')} onBlur={() => console.log('Blurred')} />
```

### 🔸 `onMouseEnter` and `onMouseLeave`
```jsx
<div onMouseEnter={() => console.log('Mouse entered')} onMouseLeave={() => console.log('Mouse left')}>
  Hover me
</div>
```

---

## ✅ Notes

- Event names use **camelCase**: `onClick`, `onSubmit`, etc.
- Event handlers are **functions**, not strings.
- React events are **synthetic** but you can access native events via `e.nativeEvent`.

---

## 🔍 Resources

- [React Docs: Handling Events](https://reactjs.org/docs/handling-events.html)





















  
