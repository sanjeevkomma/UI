# Definition
* Hoisting in JavaScript is a behavior where variable and function declarations are moved to the top of their scope (before code execution) — only the declarations, not the assignments.
